{# RETIRED (v3 merge). The separate Genomic_Variant_umbrella was folded into
   other_molecular_biomarker_umbrella — a gene sequence variant is now a
   biomarker finding carrying a nested `variant_detail` (verbatim HGVS +
   hgvs_normalized). See config/prompts/molecular_biomarker_team.j2 §3b. This
   file is no longer referenced by config/teams.yaml and is not rendered. #}
{# Intentionally left as a tombstone so the history of the merge is discoverable. #}
